var contador = 0;

while (contador < 10) {
    console.log("saludo " + contador);
    contador++;
}
var num = +prompt('Ingrese un número');
cont = 0
while (cont <= 12) {
    console.log(`${cont} x ${num} = ${cont * num}`);
    cont++
}